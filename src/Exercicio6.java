public public static void main(String[] args) {
    // Questão A
    double c = 10;
    double r = 1;
    double f = Math.sqrt((1 / (1 * c)) - 
                         (Math.pow(2, r) / Math.pow(2, (4 * c))));

    // Questão B
    double a = 1;
    double b = 4;
    double x = a / (b - c);

    // Questão C
    a = 123;
    double m = 10;
    double h = 22;
    double v = 123;
    x = m * ((a * h) + (Math.pow(2, v) / 2));

    // Questão D
    double t = 12;
    double d = (v * t) + ((a * Math.pow(2, t)) / 2);

    // Questão E
    double p = 23;
    double n = 77;
    d = (p - r) / n;

    // Questão F
    f = c * (9 / 5) + 32;

    // Questão G
    c = (f - 32) * (5 / 9);

  }
  
} 
    

